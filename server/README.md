# Server

Milestone 3b server: multi-hero + turn order, table moves active hero.

Milestone 6: Table is view-only; MOVE is performed from phone clients.

## Env config
The server loads environment variables from `server/.env` via `dotenv`.
Use `server/.env.example` as a template for public join/WS settings.
